[project]
name = "titan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "titan-core",
    "snowflake",
    "streamlit"
]

[tool.uv.sources]
titan-core = { workspace = true }
